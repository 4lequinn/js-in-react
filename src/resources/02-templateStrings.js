// Template strings 

const firstName = 'Jorge';
const lastName = 'Quintui';

//const fullName = firstName + ' ' + lastName;

const fullName = `Hello ${firstName} ${lastName}`;

console.log(fullName);
